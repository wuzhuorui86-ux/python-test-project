# simple-python-project

A simple Python project that demonstrates package structure, a tiny calculator module, and tests.

## Run tests

```bash
PYTHONPATH=src python -m unittest discover -s tests
```

## Run the CLI

```bash
PYTHONPATH=src python -m simple_project 2 3
```
